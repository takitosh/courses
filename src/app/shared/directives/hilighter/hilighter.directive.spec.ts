import { HighlightDirective } from './hilighter.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = true; // new HighlightDirective();
    expect(directive).toBeTruthy();
  });
});
